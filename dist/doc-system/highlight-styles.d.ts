import { XinStyleSheet } from 'tosijs';
/** The `.token.*` rules — light default plus a dark set under `.darkmode`. */
export declare const highlightStyleSpec: XinStyleSheet;
export declare function ensureHighlightStyles(): void;
