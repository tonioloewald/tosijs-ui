# Form Components

<!--{ "pin": "bottom", "order": 2 }-->

Form-associated custom elements — inputs, selects, and editors that participate
in native `<form>` (and `<tosi-form>`) submission and validation.

<!-- toc -->
<!-- /toc -->
