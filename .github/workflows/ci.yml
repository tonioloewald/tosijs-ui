name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bunx tsc --noEmit

      # bin/ ships as four executables and was outside the gate entirely — it had
      # accumulated two real type errors by the time a review looked.
      - name: Type check bin/
        run: bunx tsc -p tsconfig.bin.json

      # tsconfig.json EXCLUDES *.test.ts, so the vendored-type anti-drift guards — whose
      # entire value is compile-time — were never compiled. Both were inert for all of
      # 1.11.0's development and one was red. A compile-time assertion nothing compiles is
      # decoration.
      - name: Type check the vendored-type guards
        run: bun run typecheck-guards

      # The rest of the test suite, for the same reason (#73). The backlog was 48 errors,
      # now zero, and two were real defects rather than test noise: `SVGIconMap` typed
      # every icon `SVGElement` when composites return `HTMLSpanElement`, and two files
      # carried duplicate imports. Gated so it cannot silently regrow — that is the whole
      # point; the count only ever went up while nothing was watching.
      - name: Type check the test suite
        run: bunx tsc -p tsconfig.tests.json --noEmit

      # `bun format` was manual and in no gate, so drift only ever accumulated: 24
      # unformatted files at v1.9.0, 40 by v1.9.7. Nobody sees it until someone runs
      # `bun format` and gets three dozen unrelated files mixed into their feature diff.
      # Prettier reaches a fixed point in one pass and is the sole formatter (ESLint
      # carries no stylistic rules), so this check is deterministic.
      - name: Check formatting
        run: bun run format-check

      - name: Run unit tests
        run: bun test

  # The E2E lane. It used to run only when a human happened to have `bun start` up,
  # so it rotted red for ~a month across ~20 tagged releases without anyone noticing.
  # Playwright starts the dev server itself (see playwright.config.ts webServer).
  e2e:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      # The dev server serves HTTPS and refuses to start without a cert. Locally that's
      # an mkcert-trusted one; here a throwaway self-signed cert is enough, because the
      # tests already run with ignoreHTTPSErrors (and mkcert would want sudo).
      - name: Generate a throwaway dev TLS cert
        run: |
          mkdir -p tls
          openssl req -x509 -newkey rsa:2048 -nodes -days 1 \
            -keyout tls/key.pem -out tls/certificate.pem \
            -subj "/CN=localhost" \
            -addext "subjectAltName=DNS:localhost,IP:127.0.0.1"

      - name: Install Playwright browsers
        run: bunx playwright install --with-deps chromium

      - name: Run Playwright tests
        run: bunx playwright test --project=chromium

      - name: Upload report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
